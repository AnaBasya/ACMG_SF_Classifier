# ACMG_SF_Classifier - Python dependencies
# Python 3.11+ required

# Core dependencies
numpy>=1.24.0
pandas>=2.0.0

# VCF parsing and manipulation
pysam>=0.22.0

# Optional: faster VCF parsing (recommended for performance)
cyvcf2>=0.30.0

# Optional: interval tree operations for NMD prediction
intervaltree>=3.1.0

# For GraphQL API queries (gnomAD)
requests>=2.31.0
gql>=3.5.0

# JSON handling (built-in, but ensure latest)
# json (built-in)
